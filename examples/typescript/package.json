{
  "name": "helixconf-example-typescript",
  "version": "0.0.1",
  "description": "",
  "scripts": {
    "build": "rm -rf dist && tsc",
    "start": "npm run build && node dist/src/index.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "helixconf": "^0.0.5"
  },
  "devDependencies": {
    "@types/jest": "^29.5.12",
    "typescript": "^5.5.3"
  }
}
